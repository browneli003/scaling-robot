<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>F TO PAY RESPECTS</title>
  <link rel="stylesheet" href="../../public/stylesheets/style.css">
</head>

  <div class="wrapper">
    <header>
        <h1 class="">Scaling Robot blog</h1>
        <nav class="navbar mt-2">
            {{#if loggedIn}}
            <a href="/" class="">homepage</a>
            <a href="/dashboard" class="">dashboard</a>
            <a href="/dashboard/create/" class="">create post</a>
            <button id="logout" class="">logout</button>
            {{else}}
            <a href="/" class="">homepage</a>
            <a href="/login" class="">login</a>
            {{/if}}
        </nav>
    </header>

    <main>
            {{{ body }}}
    </main>

    <footer class="">
        <div class="">
            <p>
            <a href="/">homepage</a>{{#if loggedIn}} | 
            <a href="/dashboard/">dashboard</a> | 
            <a href="/dashboard/create/">create new post</a>
            {{else}}
             | <a href="/login/">login</a>
            {{/if}}
            </p>
        </div>
      <div class="">
    <p>

    </p>
  </div>
    </footer>
  </div>

{{#if loggedIn}}
<script src="../../public/javascript/logout.js"></script>
{{/if}}
</body>

</html>